<Page x:Class="Kon.Pages.PostPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Kon.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="600"
      Title="PostPage">

    <Grid>
        <DataGrid x:Name="dgPost"
                  Height="250"
                  Width="600"
                  Margin="0 0 0 50"
                  AutoGenerateColumns="False" SelectedItem="{Binding SelectedPsot}"
                  ItemsSource="{Binding View}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="Номер" Width="50"/>
                <DataGridTextColumn Binding="{Binding Title}" Header="Название" Width="80"/>
                <DataGridTextColumn Binding="{Binding Discription}" Header="Описание" Width="100"/>
                <DataGridTextColumn Binding="{Binding Salary}" Header="Описание" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button x:Name="btnDelete"
                Content="Удалить"
                Height="30"
                Width="100"
                Margin="0 270 440 0" Click="btnDelete_Click">

        </Button>
        <Button x:Name="btnEdit"
                Content="Изменить"
                Height="30"
                Width="100"
                Margin="0 270 0 0" Click="btnEdit_Click">
        </Button>
        <Button x:Name="btnAdd"
                Content="Добавить"
                Height="30"
                Width="100"
                Margin="400 270 0 0" Click="btnAdd_Click">
        </Button>
    </Grid>
</Page>
